<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My-Book-Shelf</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="styles/style.css">
</head>

<body>
  <div class="d-flex flex-column flex-md-row p-4 gap-4 py-md-5 align-items-center justify-content-center">
    <div class="list-group col-8">
      <% allbooks.forEach(book=> { %>
      <div class="list-group-item gap-3 py-3">
        <img class="float-start mx-3" src="images/9780385533225-M.jpg" alt="book-cover" width="" height="20%" >
            <h6 class="mb-0 mx-3"><%= book.title %></h6>
            <p class="mb-0 mx-3 opacity-75"><%= book.lastedited %> How strongly I recommend it: <%= book.rating %>/10</p>
            <p class="mb-0 mx-3 description"><%= book.description %></p>
      </div>
      <% }); %>
    </div>
  </div>
  
  <h1 class="title">hi there</h1>
  <% allbooks.forEach(book=> { %>
    <div class="book">
      <img src="" alt="" class="cover">
      <p class="title">
        <%= book.title %>
      </p>
      <p class="lastedited">
        <%= book.lastedited %>
      </p>
      <p class="rating">
        <%= book.rating %>
      </p>
      <p class="description">
        <%= book.description %>
      </p>
      <p class="notes">
        <%= book.notes %>
      </p>
    </div>
    <% }); %>
</body>

</html>
